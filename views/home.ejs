<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - YouTube Favorites</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            overflow: hidden;
        }

        .home-container {
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 40px;
        }

        .home-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            overflow: hidden;
            animation: slideUp 0.5s ease-out;
            max-width: 650px;
            width: 100%;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .home-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 50px 30px;
            text-align: center;
        }

        .home-header h1 {
            margin: 0;
            font-size: 42px;
            font-weight: 700;
        }

        .home-header p {
            margin: 15px 0 0 0;
            font-size: 18px;
            opacity: 0.9;
        }

        .home-body {
            padding: 40px 30px;
        }

        .user-info {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 30px;
            text-align: center;
        }

        .user-avatar {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 15px;
            font-size: 36px;
            color: white;
        }

        .user-name {
            font-size: 24px;
            font-weight: 700;
            color: #333;
            margin-bottom: 5px;
        }

        .user-email {
            color: #666;
            font-size: 14px;
        }

        .action-buttons {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .btn-action {
            padding: 18px;
            border-radius: 12px;
            font-weight: 600;
            font-size: 16px;
            border: none;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .btn-favorites {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .btn-favorites:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
            color: white;
        }

        .btn-logout {
            background: white;
            color: #dc3545;
            border: 2px solid #dc3545;
        }

        .btn-logout:hover {
            background: #dc3545;
            color: white;
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(220, 53, 69, 0.3);
        }

        .feature-list {
            margin-top: 20px;
            padding: 0;
            list-style: none;
        }

        .feature-list li {
            padding: 12px 0;
            border-bottom: 1px solid #e0e0e0;
            display: flex;
            align-items: center;
            gap: 12px;
            color: #666;
        }

        .feature-list li:last-child {
            border-bottom: none;
        }

        .feature-list i {
            color: #667eea;
            font-size: 20px;
        }
    </style>
</head>

<body>
    <div class="home-container">
        <div class="home-card">
            <div class="home-header">
                <h1>
                    <i class="bi bi-house-heart-fill"></i>
                </h1>
                <h1>Welcome Home</h1>
                <p>Your personal YouTube favorites hub</p>
            </div>

            <div class="home-body">
                <div class="user-info">
                    <div class="user-avatar">
                        <i class="bi bi-person-fill"></i>
                    </div>
                    <div class="user-name"><%= user.fullName %></div>
                    <div class="user-email"><%= user.email %></div>
                </div>

                <div class="action-buttons">
                    <a href="/favorites" class="btn btn-action btn-favorites">
                        <i class="bi bi-youtube"></i>
                        <span>My YouTube Favorites</span>
                    </a>

                    <form method="POST" action="/logout" class="m-0">
                        <button type="submit" class="btn btn-action btn-logout w-100">
                            <i class="bi bi-box-arrow-right"></i>
                            <span>Logout</span>
                        </button>
                    </form>
                </div>

                <ul class="feature-list">
                    <li>
                        <i class="bi bi-search"></i>
                        <span>Search YouTube videos</span>
                    </li>
                    <li>
                        <i class="bi bi-heart-fill"></i>
                        <span>Save your favorite videos</span>
                    </li>
                    <li>
                        <i class="bi bi-arrows-move"></i>
                        <span>Drag & drop to reorder</span>
                    </li>
                    <li>
                        <i class="bi bi-play-circle"></i>
                        <span>Watch anytime, anywhere</span>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
